name: 🤖 Weekly AI Review

on:
  schedule:
    - cron: '0 10 * * 1'  # 每周一18:00
  workflow_dispatch:

jobs:
  review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        working-directory: scripts
        run: npm install

      - name: Generate weekly report
        env:
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
        working-directory: scripts
        run: npx tsx ai-review.ts

      - name: Refresh README dashboard
        working-directory: scripts
        run: npx tsx dashboard.ts

      - name: Commit report
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add reviews/weekly/
          git add README.md
          git commit -m "🤖 Weekly AI Review - $(date +%Y-%m-%d)" || echo "No changes"
          git push
